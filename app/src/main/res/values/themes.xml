<resources>
<style name="Theme.ClientApp" parent="Theme.AppCompat.DayNight.NoActionBar">
    <!-- Mapeia suas cores semânticas para os tokens de tema do AppCompat -->
    <item name="colorPrimary">@color/corFundo</item>
    <item name="colorAccent">@color/corTexto</item>
    <!-- Fundo da janela/tela -->
    <item name="android:windowBackground">@color/corFundo</item>
    <!-- Expondo suas cores "on*" como atributos do tema -->
    <item name="colorOnPrimary">@color/corFundo</item>
    <item name="colorOnSecondary">@color/corTexto</item>
</style>
    </resources>
